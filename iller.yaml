---
doctype: dist

header:
  name: Map-Metro-Plugin-Map-Madrid

prereqs:
  runtime:
    requires:

plugins:
  - +config: Author::CSSON

  - +extend_plugin: Git::GatherDir
	exclude_match: \.png

  - +plugin: MapMetro::MakeGraphViz
  
  - +plugin: MapMetro::MakeLinePod

---
doctype: weaver

plugins:
 - +config: Author::CSSON

 - +remove_plugin: Splint
